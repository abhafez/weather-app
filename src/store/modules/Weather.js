// import axios from 'axios';

const state = {
  currentCityTemp: 'hello',
};

const getters = {
  currentCityTemp: state => state.currentCityTemp,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
