<template>
  <div id="app" class="container mx-auto">
    <nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6">
      <!-- LOGO -->
      <div class="flex items-center flex-shrink-0 text-white mr-6">
        <img src="./assets/logo.svg" alt="Weather" />
        <span class="font-semibold text-xl tracking-tight">Weather</span>
      </div>
      <!-- LOGO END -->
      <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">
          <router-link to="/">
            <a
              class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4"
            >
              Home
            </a>
          </router-link>
        </div>
      </div>
      <!-- CELSIUS / FAHR convert -->
      <button
        href="#"
        class="inline-block text-sm px-4 py-2 leading-none border rounded text-white border-white hover:border-transparent hover:text-teal-500 hover:bg-white mt-4 lg:mt-0"
        @click="isCelsius ? convertoToFahr() : convertoToCelisus()"
      >
        <span :class="isCelsius ? 'text-red-700' : 'text-gray-900'">C</span>
        /
        <span :class="!isCelsius ? 'text-red-700' : 'text-gray-900'">F</span>
      </button>
    </nav>
    <router-view />
  </div>
</template>

<script>
import { mapActions, mapGetters } from 'vuex';

export default {
  methods: {
    ...mapActions(['convertoToCelisus', 'convertoToFahr']),
  },
  computed: {
    ...mapGetters(['isCelsius']),
  },
};
</script>
